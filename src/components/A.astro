---
import type { HTMLAttributes } from "astro/types";

interface Props extends HTMLAttributes<"a"> {
  class?: string;
}

const { class: className, ...rest } = Astro.props;
---

<a class:list={["text-azure-3 hover:underline", className]} {...rest}>
  <slot />
</a>
